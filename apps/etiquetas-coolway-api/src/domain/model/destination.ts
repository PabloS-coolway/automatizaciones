/**
 * Preset destino → variante + importado por (RF-14).
 * Definido en el paquete de contratos compartido para que la API y el front usen el mismo.
 */
export { MARKETS, MARKET_CODES, resolveMarket } from '@yorga/contracts';
export type { MarketCode, MarketPreset } from '@yorga/contracts';
